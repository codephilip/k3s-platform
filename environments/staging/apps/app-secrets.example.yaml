# Template for the app-secrets Secret consumed by charts/example-app.
# See environments/dev/apps/app-secrets.example.yaml for full notes.
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: staging-apps
type: Opaque
stringData: {}
  # DATABASE_URL: "..."
  # STRIPE_SECRET_KEY: "..."
