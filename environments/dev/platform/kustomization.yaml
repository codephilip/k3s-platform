# Monitoring: install kube-prometheus-stack in observability namespace via Helm, e.g.:
#   helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
#   helm upgrade --install platform-tools-dev prometheus-community/kube-prometheus-stack -n observability
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
