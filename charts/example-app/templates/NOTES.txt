{{ .Chart.Name }} ({{ .Release.Name }}) deployed to namespace {{ .Release.Namespace }}.

{{- if .Values.ingress.enabled }}

Hosts:
{{- range .Values.ingress.hosts }}
  https://{{ .host }}
{{- end }}

TLS issuer: {{ index .Values.ingress.annotations "cert-manager.io/cluster-issuer" | default "(none)" }}

Check rollout:
  kubectl -n {{ .Release.Namespace }} rollout status deploy/{{ include "example-app.fullname" . }}

Check cert issuance:
  kubectl -n {{ .Release.Namespace }} get certificate
  kubectl -n {{ .Release.Namespace }} describe certificate
{{- else }}

Ingress is disabled. Port-forward to test:
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "example-app.fullname" . }} 8080:{{ .Values.service.port }}
{{- end }}
